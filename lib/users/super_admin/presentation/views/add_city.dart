import 'package:flutter/material.dart';

class AddCity extends StatelessWidget {
  const AddCity({super.key});
  static String id = 'AddCity';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Column(
        children: [





        ],
      ),
    );
  }
}
